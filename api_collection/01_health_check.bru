meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{base_url}}/health
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

docs {
  # Health Check
  
  Check if the API service is running and healthy.
  
  ## Expected Response
  ```json
  {
    "status": "healthy"
  }
  ```
}
