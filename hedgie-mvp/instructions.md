1) If you have some questions in order to perform your task better, ask them before generating an answer. It is better to have everything clear.
2) Consider that we will want to dockerize this application.
3) There should be a separated backend and frontend. The frontend may be an app or a web page.
4) Consider that we will use FastAPI and React with Material UI. We will implement the frontend later, so for now, only do a minimalistic frontend.
5) Try to use the latest documentation and generate tests for each implementation.
6) Generate a .md file in the /docs folder for each step you take in the tasks list, explaining your decisions.
7) Try not to generate too much of the data model at once. Let's advance with the data model when we need it for each step.
8) For each implementation we will do, add tests for our backend. Data should be mocked